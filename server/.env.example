# Database
# For local development with PostgreSQL:
DATABASE_URL="postgresql://user:password@localhost:5432/mascotas_db?schema=public"

# For Railway (automatically provided):
# DATABASE_URL will be set by Railway when you connect a PostgreSQL database

# JWT Secret (change in production!)
JWT_SECRET="your-secret-key-change-in-production-use-long-random-string"

# Server
PORT=3000

# Node Environment
NODE_ENV=production
